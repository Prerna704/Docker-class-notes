/**
create a python class web app =--->run a python web application inside docker so it works anywhere without setup issues

   mkdir folder name
   cd foldername
   create three files- app.py, requirements.txt,dockerfile
   in app.py write 
   from flask import Flask
app = Flask(__name__)

@app.route("/")
def home():
    return "Hello from Flask Docker App"

app.run(host="0.0.0.0", port=5000)
  in requirements u should only write flask


  in dockerfile you should write
  FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
CMD ["python", "app.py"]

flask used for your web framework -- docker will install all dependencies automatically

@app.route("/") - used for your home page
host="0.0.0.0" ===>>>>accessible from the outside of container


*/